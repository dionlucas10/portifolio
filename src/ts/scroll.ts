import ScrollReveal from 'scrollreveal';

const slideUp = {
  distance: '150%',
  origin: 'bottom',
  duration: 1000,
};

const sr = ScrollReveal();

sr.reveal('.tecnologias', slideUp);
sr.reveal('.habilidades', slideUp);
sr.reveal('.projetos', slideUp);
sr.reveal('.swiper', slideUp);
sr.reveal('.contene', slideUp);
sr.reveal('.certificados', slideUp);